/**
 * @fileoverview Agents page loading component
 * Uses the existing AgentLoading component with skeleton-based UI
 */

import { AgentLoading } from "@/components/agents/AgentLoading";

export default function Loading() {
  return <AgentLoading />;
} 