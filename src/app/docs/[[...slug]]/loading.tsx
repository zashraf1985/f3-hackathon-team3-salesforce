/**
 * @fileoverview Documentation page loading component
 * Uses the DocsSkeleton component that matches the documentation structure
 */

import { DocsSkeleton } from "@/components/docs/DocsSkeleton";

export default function Loading() {
  return <DocsSkeleton />;
} 