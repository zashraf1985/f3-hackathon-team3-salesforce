# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/agentdock
# Set to 'production' for Vercel/Fly.io deployments
NODE_ENV=development

# Deployment Configuration
DEPLOYMENT_TYPE=oss

# Service Provider Configuration
# Options: 'openrouter', 'google', 'none'
SERVICE_PROVIDER=none

# Service Provider API Keys
# Only required if SERVICE_PROVIDER is set to the corresponding provider
OPENROUTER_API_KEY=
GOOGLE_API_KEY=

# Rate Limiting
# Only applies to service provider keys, not BYOK
ENABLE_RATE_LIMIT=no

# Note: For production deployments:
# - Set NODE_ENV=production
# - Configure appropriate SERVICE_PROVIDER
# - Add corresponding API key
# - Consider enabling rate limiting with ENABLE_RATE_LIMIT=yes

# - Vercel: Configure DATABASE_URL in the project settings
# - Fly.io: Use `fly secrets set DATABASE_URL=...` 